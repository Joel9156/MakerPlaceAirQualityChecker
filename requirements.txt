Flask>=3.0

# Real-hardware dependencies. Not required in mock mode, but pip install
# will fail on non-Pi/non-Linux machines if these are forced, so they are
# only actually imported by sensors.py when mock mode is off.
pyserial>=3.5
smbus2>=0.4.3
bme680>=1.1.1
