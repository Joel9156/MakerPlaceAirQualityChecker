Flask>=3.0

# Real-hardware dependency (talks to the Pico over USB serial). Not required
# in mock mode - only imported by sensors.py when mock mode is off. All
# sensors (including the BME680) are wired to the Pico, not the Pi, so no
# I2C library (smbus2/bme680) is needed on the Pi side.
pyserial>=3.5
